[Unit]
Description=OPS memory-pool inventory (ac-memory-gc, read-only) — weekly staged report

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'mkdir -p %h/.local/state/ops && %h/OPS/WORKFORCE/bin/ac-memory-gc > %h/.local/state/ops/memory-gc-last.txt 2>&1; exit 0'
